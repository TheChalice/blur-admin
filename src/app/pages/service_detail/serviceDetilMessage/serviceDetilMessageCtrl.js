/** * Created by sorcerer on 2017/11/1. */(function () {    'use strict';    angular.module('BlurAdmin.pages.servicedetil')        .controller('serviceDetilMessageCtrl', serviceDetilMessageCtrl);    /** @ngInject */    function serviceDetilMessageCtrl($scope,ContainerModal) {        $scope.containerModal = function (name, idx) {            //var o = $scope.pods.items[idx];            var obj = {};            angular.forEach($scope.pods.items, function (pod, i) {                if (pod.metadata.name == name) {                    obj = pod                }            })            var pod = obj.spec.containers[idx];            ContainerModal.open(obj, pod);        };    }})();