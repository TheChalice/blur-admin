/** * @author v.lugovksy * created on 16.12.2015 */(function () {    'use strict';    angular.module('BlurAdmin.theme.components')        .controller('pageTopCtrl', pageTopCtrl);    /** @ngInject */    function pageTopCtrl($scope,Cookie,$rootScope,$state) {        if (!$rootScope.namespace) {            $rootScope.namespace=Cookie.get('namespace')        }        $scope.signout= function () {            Cookie.clear('df_access_token');            Cookie.clear('namespace');            Cookie.clear('region');            $rootScope.region = '';            $scope.checked = '';            $rootScope.user = null;            $rootScope.namespace = "";            //clearInterval($scope.timer);            $state.go('login');        }    }})();